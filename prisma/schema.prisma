// Prisma schema for social-media-crm.
// Models land in Day 2 Phase 2 (identity) and Phase 3 (posts/schedule).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
